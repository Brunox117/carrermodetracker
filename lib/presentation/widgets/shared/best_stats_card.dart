import 'package:flutter/material.dart';

class BestStatsCard extends StatelessWidget {
  const BestStatsCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}