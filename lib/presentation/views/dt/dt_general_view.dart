
import 'package:flutter/material.dart';

class DtGeneralView extends StatelessWidget {
  const DtGeneralView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DtGeneralView'),
      ),
      body: const Center(
        child: Text('DtGeneralView'),
      ),
    );
  }
}